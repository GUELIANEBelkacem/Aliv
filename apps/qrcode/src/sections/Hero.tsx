export function Hero() {
  return (
    <header className="qr-hero">
      <h1 className="qr-hero-title">Customizable QR codes. Free, no login, no watermark.</h1>
      <p className="qr-hero-sub">
        Wi-Fi, vCard, URL, calendar — generate, color, shape, brand with a logo,
        and export as PNG or SVG. Everything runs in your browser; nothing
        uploads.
      </p>
    </header>
  );
}
