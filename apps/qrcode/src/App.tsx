import { AppShell } from '@aliv/ui';

export default function App() {
  return (
    <AppShell appId="qrcode">
      <main className="qr-app">
        <p className="qr-placeholder">QR generator content lands in Q.1+</p>
      </main>
    </AppShell>
  );
}
